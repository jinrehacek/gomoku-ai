[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gomoku"
version = "0.1"
dependencies = ["rich"]

[project.optional-dependencies]
dev = ["pytest", "ruff"]















